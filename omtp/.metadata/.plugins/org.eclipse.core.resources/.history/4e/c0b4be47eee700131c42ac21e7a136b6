package asd01;

@license(LGPL);

simple MobileNode {
	gates:
	    input in;
	    output out;
}

simple HomeAgent {
	gates:
	    input in;
	    output out;
}

network asd01 {
	submodules:
	    mn: MobileNode;
	    ha: HomeAgent;
}